export { List, Map } from 'immutable';
/* const l = [{ a: 1 }, { b: 1 }];
const simply = [2,1, 3];C
const list = List(l);
const list1 = List(simply);
const filter=(a)=>{
    console.log('111111111');
    return a>1;
}
// list1.filter(filter)
console.log(list1.toSeq().filter(filter));
console.log(list1.sort()) */
